<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin-Category</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body class="bg-gray-100">
    <div class="flex min-h-screen">

        <%- include("../partials/admin/sidebar") %>
            <div class="flex flex-col flex-1 ml-64">
                <header
                    class="sticky top-0 z-20 p-6 bg-white shadow flex flex-wrap justify-between items-center border-b border-gray-200">
                    <h1 class="text-2xl font-bold text-gray-800">Category</h1>
                    <form method="GET" action="/admin/category" class="flex items-center gap-2 mt-4 md:mt-0">
                        <input type="text" name="search" value="<%search%>" placeholder="Search here"
                            class="border border-gray-300 rounded px-3 py-2 focus:ring-2 focus:ring-yellow-400 focus:outline-none w-60">
                        <button type="submit"
                            class="px-4 py-2 bg-yellow-400 text-white rounded hover:bg-yellow-500 transition duration-200">
                            Search
                        </button>
                        <a href="/admin/category"
                            class="px-4 py-2 bg-gray-300 text-gray-800 rounded hover:bg-gray-400 transition duration-200">Clear</a>

                    </form>
                </header>

                <main class="p-6 flex-1">
                    <div class="bg-white shadow rounded-lg p-4 overflow-x-auto">


                        <div class="flex gap-2 justify-end mb-3">
                            <button onclick="openModal('add')"
                                class="bg-gray-900 text-white px-3 py-1.5 rounded hover:bg-gray-900 text-sm transition">
                                + Add New
                            </button>
                            <button
                                class="bg-gray-900 text-white px-3 py-1.5 rounded hover:bg-gray-900 text-sm transition">
                                + Add Offer
                            </button>
                        </div>


                        <table class="w-full border-collapse">
                            <thead>
                                <tr class="border-b bg-gray-200">
                                    <th class="text-left px-4 py-2 font-semibold text-gray-700">Category</th>
                                    <th class="text-center px-4 py-2 font-semibold text-gray-700">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <%categories.forEach(category=>{%>
                                    <tr class="border-b hover:bg-gray-50 transition">

                                        <td class="px-4 py-2">
                                            <%=category.name%>
                                        </td>
                                        <td class="px-4 py-2 text-center">
                                         <button onclick='openModal("edit", JSON.parse(decodeURIComponent("<%= encodeURIComponent(JSON.stringify(category)) %>")))'
        class="text-red-500 hover:text-red-700 mx-1">✏️</button>

<button onclick='openModal("delete", JSON.parse(decodeURIComponent("<%= encodeURIComponent(JSON.stringify(category)) %>")))'
        class="text-blue-500 hover:text-blue-700 mx-1">🗑</button>
   


                                            

                                        </td>
                                    </tr>

                                    <%})%>
                            </tbody>
                        </table>
                    </div>
                    <!-- modal -->

                    <div id="categoryModal"
                        class="hidden fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center z-50">

                        <div class="bg-white rounded-lg shadow-lg w-full max-w-lg p-6 relative">

                            <h2 id="modalTitle"
                                class="text-xl font-semibold text-center bg-black text-white py-2 rounded">Add Category
                            </h2>


                            <form id="modalForm" class="mt-4 space-y-4" enctype="multipart/form-data">
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                    <div>
                                        <label for="name" class="block text-sm font-medium text-gray-700">Category
                                            Name</label>
                                        <input type="text" id="name" name="name"
                                            class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring focus:ring-blue-200"
                                            placeholder="Enter category name" >
                                    </div>

                                    <div>
                                        <label for="image" class="block text-sm font-medium text-gray-700">Image</label>
                                        <input type="file" id="image" name="image"
                                            class="w-full border border-gray-300 rounded px-3 py-2 file:mr-3 file:py-2 file:px-4 file:rounded file:border-0 file:text-sm file:bg-gray-900 file:text-white hover:file:bg-gray-700" />
                                    </div>
                                </div>

                                <div>
                                    <label for="description"
                                        class="block text-sm font-medium text-gray-700">Description</label>
                                    <textarea id="description" name="description"
                                        class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring focus:ring-blue-200"
                                        rows="3" placeholder="Enter category description"></textarea>
                                </div>

                                
                                <p id="formError" class="text-red-600 text-sm hidden">All fields are required.</p>
                                <div class="flex justify-center space-x-3 mt-4">
                                    <button type="submit" id="submitBtn"
                                        class="bg-black text-white px-5 py-2 rounded hover:bg-gray-800 transition">Save</button>
                                    <button type="button" onclick="closeModal()"
                                        class="border border-gray-500 text-gray-700 px-5 py-2 rounded hover:bg-gray-100 transition">Cancel</button>
                                </div>
                            </form>
                        </div>
                    </div>



                    <div class="flex justify-end mt-6">
                        <a href="/admin/SubCategory"
                            class="bg-yellow-500 text-white px-4 py-2 rounded hover:bg-purple-600 transition">
                            + Add SubCategory
                        </a>
                    </div>
                  <!-- pagination -->
                    <div class="mt-4 flex justify-center items-center space-x-2">
                        <a href="/admin/category?page=<%= page - 1 %>&search=<%= search %>"
                            class="px-3 py-1 border rounded hover:bg-gray-200 <%= page === 1 ? 'pointer-events-none opacity-50' : '' %>">
                            Prev
                        </a>
                        <% for (let i=1; i <=totalPages; i++) { %>
                        <a href="/admin/category?page=<%= i %>&search=<%= search %>"
                                class="px-3 py-1 border rounded <%= page === i ? 'bg-yellow-500 text-white' : 'hover:bg-gray-200' %>">
                                <%= i %>
                        </a>
                            <% } %>
                        <a href="/admin/category?page=<%= page + 1 %>&search=<%= search %>"
                                    class="px-3 py-1 border rounded hover:bg-gray-200 <%= page === totalPages ? 'pointer-events-none opacity-50' : '' %>">
                                    Next
                        </a>
                    </div>

                </main>
                <%- include("../partials/admin/footer") %>
</body>
<script>

    const modal = document.getElementById('categoryModal')
    const title = document.getElementById('modalTitle')
    const form = document.getElementById('modalForm')
    const nameInput = document.getElementById('name')
    const imageInput = document.getElementById('image')
    const descriptionInput = document.getElementById('description')
    const errorMsg = document.getElementById('formError')
    const submitBtn = document.getElementById('submitBtn')

    let actionType = "add"
    let editCategoryId = null

    function openModal(type, category = null) {
        actionType = type
        modal.classList.remove("hidden")
        form.reset()
        errorMsg.classList.add("hidden")

        if (type === 'add') {
            title.textContent = 'Add Category'
            submitBtn.textContent = 'Add'
        }
        else if (type === 'edit') {
            title.textContent = 'Edit Category'
            submitBtn.textContent = 'Edit'
            if (category) {
                editCategoryId = category._id
                nameInput.value = category.name
                descriptionInput.value = category.description
            }
        }
        else if (type === 'delete') {
            title.textContent = 'Delete Category'
            submitBtn.textContent = 'Delete'
            if(category){
                editCategoryId=category._id
                nameInput.value=category.name
                descriptionInput.value = category.description

            }
            nameInput.disabled = true
            descriptionInput.disabled = true
        }
    }

    function closeModal() {
        modal.classList.add("hidden")
        editCategoryId = null
    }

    form.addEventListener('submit', async (e) => {
        e.preventDefault()

        const name = nameInput.value.trim()
        const description = descriptionInput.value.trim()
        const image = imageInput.files[0]

        if (!name || !description) {
            errorMsg.textContent = "Name and Description are required."
            errorMsg.classList.remove("hidden")
            return
        }

        const formData = new FormData()
        formData.append("name", name)
        formData.append("description", description)
        if (image) formData.append("image", image)

        let url = `/admin/category/add`
        let method = "POST"

        if (actionType === 'edit') {
            url = `/admin/category/edit/${editCategoryId}`
            method = "PATCH"
        }
        if (actionType === 'delete') {
            url = `/admin/category/delete/${editCategoryId}`
            method = "PATCH"
         }

        try {
            const res = await fetch(url, { 
                method,
                body: formData })
            const result = await res.json()

            if (result.success) {
                closeModal()
                location.reload()
            } else {
                errorMsg.textContent = result.message 
                errorMsg.classList.remove("hidden")
            }
        } catch (error) {
            console.error(error)
            errorMsg.textContent = "Server error"
            errorMsg.classList.remove("hidden")
        }
    });
</script>







</html>